# Copy to `.env` and change values for your local machine.
TRADER_DEV_MODE=true
VITE_API_BASE_URL=http://127.0.0.1:8000
# Remote Control (receiver token, enabled flag, receiver URL) is configured from
# the Remote Control page in-app and stored in config.json -- no env vars needed.
